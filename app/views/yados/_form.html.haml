= form_for @yado, html: { class: "form form-horizontal yado" } do |f|
  .form-group
    = f.label :name, class: 'control-label col-lg-2'
    .col-lg-10
      = f.text_field :name, class: 'form-control', required: true
    = f.error_span(:name)
  .form-group
    = f.label :short_name, class: 'control-label col-lg-2'
    .col-lg-10
      = f.text_field :short_name, class: 'form-control', required: true
    = f.error_span(:short_name)
  .form-group
    .col-lg-offset-2.col-lg-10
      = f.submit nil,class: 'btn btn-primary'
      = link_to t('.cancel', :default => t("helpers.links.cancel")), yados_path, :class => 'btn btn-default'
